To activate logging for an endpoint, you have to check the `Log Requests` checkbox in
the endpoint's configuration. This will log all requests and responses for that
endpoint.

A smart button will be displayed in the endpoint's form view to access the endpoint
logs. A global log view is also available in the `FastAPI Logs` menu.
