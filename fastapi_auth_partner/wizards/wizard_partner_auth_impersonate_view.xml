<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <record id="wizard_partner_auth_impersonate_view_form" model="ir.ui.view">
        <field name="model">wizard.partner.auth.impersonate</field>
        <field name="arch" type="xml">
            <form string="Label">
              Please choose an endpoint:
                <group>
                    <field name="fastapi_auth_partner_id" readonly="1" />
                    <field name="fastapi_auth_directory_id" invisible="1" />
                    <field
                        name="fastapi_endpoint_id"
                        domain="[('directory_id', '=', fastapi_auth_directory_id)]"
                    />
                </group>
                <footer>
                    <button
                        name="action_impersonate"
                        string="Impersonate"
                        type="object"
                        class="oe_highlight"
                    />
                    <button string="Cancel" class="oe_link" special="cancel" />
                </footer>

            </form>
        </field>
    </record>

    <record id="fastapi_auth_partner_action_impersonate" model="ir.actions.act_window">
        <field name="name">Impersonate</field>
        <field name="res_model">wizard.partner.auth.impersonate</field>
        <field name="type">ir.actions.act_window</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="groups_id" eval="[(4, ref('group_partner_auth_manager'))]" />
    </record>

</odoo>
