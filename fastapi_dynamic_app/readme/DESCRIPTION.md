This module allows to configure fastapi endpoints directly from the odoo interface. It
makes it possible to manage the endpoint routers and their options, such as the prefix
and the authentication method.

It also provide some demo authentication methods.
